// Web storage implementation export
import { WebStorageProvider } from '@shared/persistence/storageService';

// Export the web storage provider for use in the app
export const webStorageProvider = new WebStorageProvider();
